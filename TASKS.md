# TASKS.md — WebStaffr 3.0

Working task list for the intake → generated customer site → Angel widget MVP flow (see `CLAUDE.md` for full operational history/decisions, `PROJECT.md` for product vision). Kept here so state survives across chat sessions.

Last updated: 2026-07-12.

## Completed

- #1–#7 — Backend intake flow: `intake_submissions` migration, `POST /intake`, tests, `GET /sites/{tenant_id}`, Lovable "Site Weaver" multi-tenant site project created.
- #8 — Hosting decision made and executed: Supabase Postgres + Vercel (reconciled from `[pending]` — the decision was made and largely built out earlier, just hadn't been marked done).
- #9 — Vercel FastAPI/ASGI support confirmed working (via root `index.py` entrypoint, not `pyproject.toml` — that approach broke the build).
- #10 — Supabase Postgres project set up, migrations `0001`–`0004` applied (including RLS default-deny).
- #11, #14–#19 — Dual-backend (SQLite/Postgres) persistence layer built, RLS applied, `psycopg2-binary` added, full 81-test suite passing against SQLite.
- #13 — Lovable site pointed at the real deployed backend URL. Lovable's agent worked around a reserved `VITE_` secret-prefix restriction by using `API_BASE_URL` (with `VITE_API_BASE_URL` kept as a fallback) and updating `vite.config.ts`'s `envPrefix`. Verified live preview still renders.
- #20 — Fixed a real, unrelated bug: `requirements-dev.txt` pinned `pytest==9.1.1`, which doesn't exist on PyPI, silently breaking the README's documented fresh-install steps. Repinned to real `8.4.2`. Also discovered no `.venv` actually existed locally despite prior "81/81 passing" claims — built one for real and reverified.
- #21 — Fixed a real production bug found via `get_runtime_errors`: the ASGI lifespan handler was opening a DB connection at cold start just to call `migrate()` (a documented no-op under Postgres), meaning the Supabase outage was crashing the *entire* app — including `/health` — not just DB-touching routes. Fixed to skip that connection under Postgres. Also fixed 5 route handlers that let raw DB exceptions (including the Supavisor pooler hostname) leak to clients instead of returning a clean 503. **Verified live**: `/health` now returns `200` on the real Vercel deployment right now, while the Supabase incident is still open.
- #22 — Retell AI voice/telephony integration (`retell.py`, `retell_router.py`: signature verification, `/retell/webhook`, `/retell/function-call`), committed and pushed as `1705b71` (2026-07-08). This line was missing from TASKS.md until now — CLAUDE.md's addendum said it had been logged here but never was; caught while editing this section for #23 below. See CLAUDE.md's matching 2026-07-08 addendum for full detail.
- #23 — First `CODE_REVIEW.md` fix (High severity, item #1 of its Prioritized Action List): added minimal shared-secret auth to `/book` (`X-API-Key` / `BOOK_API_KEY`) and `/webhooks/ghl` (`X-Webhook-Secret` / `GHL_WEBHOOK_SECRET`) — new `webstaffr/workers/angel/api_auth.py`, same Null-verifier-until-configured pattern as Retell's own webhook verification. 110/110 tests passing (102 prior + 8 new), health check HEALTHY. Committed and pushed as part of `e0088f0` (see below).
- #24 — Second `CODE_REVIEW.md` fix (High severity, item #2): (a) `Field(max_length=4000)` on `ChatRequest.message`/`GHLWebhookEvent.message` — previously unbounded, a real billed-xAI-cost risk now that `GROK_API_KEY` is live; (b) per-tenant, per-endpoint rate limiting on `/chat`/`/webhooks/ghl` (new `webstaffr/rate_limit.py` + migration `0005_rate_limit_counters.sql`), 429 on exceeding 30 requests/60s. Founder chose "best practices" and left the specific approach to Claude's judgment: picked a DB-backed fixed-window counter (reuses existing `db.py`, no new external vendor) over an in-memory counter, because this app runs as Vercel serverless functions with no persistent process — an in-memory counter would not enforce a real limit across Vercel's multiple/cold-started instances, only a DB-backed one does. 121/121 tests passing (110 prior + 11 new), health check HEALTHY. Committed as `f3cd6b8`, pushed.
- #25 — Third `CODE_REVIEW.md` fix (Medium, action item #4): committed the RLS migration as a real file — `webstaffr/migrations/postgres_manual/0004_enable_rls_default_deny.sql`. Deliberately placed in a subdirectory, not a direct sibling of `0001`–`0003`/`0005`: `db.py`'s `migrate()` globs every `*.sql` file directly under `webstaffr/migrations/` and runs it against SQLite for local dev/tests, and `ENABLE ROW LEVEL SECURITY` is Postgres-only syntax with no SQLite equivalent — a sibling file would have broken the entire test suite. Verified via `Path.glob("*.sql")` directly that the subdirectory file is invisible to that glob (121/121 still passing after adding it). The original literal SQL text applied 2026-07-07 isn't retrievable via the Supabase MCP's `list_migrations` (version+name only, no body), so the committed file is a reconstruction — verified against live state before committing: `pg_tables`/`pg_class` show `rowsecurity = true` on all 5 tables, `pg_policies` is empty (confirms default-deny), `get_advisors` shows only the 5 expected INFO-level "RLS Enabled No Policy" notices, and the exact statements in the committed file were re-executed against the live project as a verification step (idempotent, no-op, safe). Committed as `5bf1810`, pushed.
- #26 — Fourth `CODE_REVIEW.md` fix (Medium, action item #3): founder's decision was **remove** — `license_number` no longer appears in `GET /sites/{tenant_id}`'s public response at all (was previously always included, unlike the optional fields). Added to `test_site_data.py`'s `_INTERNAL_FIELDS` "must never leak" list, same pattern as the earlier `competitors` leak fix. `webstaffr/site_data.py`'s docstring documents why (Supabase's advisor had already flagged this column sensitive; founder's call to pull it rather than keep it as a trust-signal display field). 121/121 tests passing, health check HEALTHY, `scripts/onboarding_smoketest.py` 12/12. Committed as `dce44f8`, pushed.
- #27 — Fifth and final `CODE_REVIEW.md` fix (Medium, action item #5): new `tests/test_db_pg_shim.py`, 15 tests exercising `_PGConnection.execute()`'s SQLite-to-Postgres dialect translation directly — the `INSERT OR IGNORE` → `ON CONFLICT DO NOTHING` rewrite, the hardcoded `INSERT OR REPLACE INTO workflow_definitions` translation, the `RETURNING <pk>` auto-append for `_LASTROWID_PK` tables (and confirms it's *not* double-appended when a query already has `RETURNING`), the `PRAGMA` no-op, the `?`→`%s` rewrite, and `commit`/`rollback`/`close`/`executescript` pass-through. No real Postgres connection needed — a fake object shaped like psycopg2's connection/cursor surface records what SQL text/params it receives. This was previously the one piece of dialect-sensitive code with zero test coverage of any kind (see CLAUDE.md's 2026-07-08 Retell addendum). 136/136 tests passing (121 prior + 15 new), health check HEALTHY, onboarding smoketest 12/12. Committed as `e0088f0`, pushed.

- **Local Vite+React perfect-site frontend** added under `frontend/`. One canonical page: `src/SitePage.tsx`, pulling live tenant data via `src/lib/site-api.ts`, with industry-specific theming in `src/lib/theme.ts`. This replaces the Lovable-agent credit-burning workflow for frontend edits; future changes are normal commits in this repo. Committed as `ec02df1`, pushed.
- **Voice-agent work completed** via subagent on July 12: added spoken-time normalization in `webstaffr/workers/angel/booking.py` so voice bookings handle phrases like "3pm", "today at 3pm", and ISO strings; tightened retell router to reject whitespace-only `preferred_time`; added 17 targeted tests. Local verification reported 136/136 passing and HEALTHY. Part of `ec02df1`, pushed.

**`CODE_REVIEW.md` is now fully worked through and pushed — all 5 action items resolved this session (#23–#27).** Pushed to `origin/main` as `e0088f0` (2026-07-08), verified directly via `git ls-remote origin main` (matches local `HEAD` exactly) and `git status --short --branch` (no ahead/behind). `CODE_REVIEW.md` itself remains untracked/uncommitted — that's still a separate, open founder decision, distinct from the 5 numbered fixes it describes.
- #28 (was blocked item #12) — **Supabase `ap-south-1` incident resolved; `DATABASE_URL` now authenticating in production.** `status.supabase.com` showed the incident still technically "Monitoring" (not formally Resolved) but the component board green across all regions — that alone did **not** mean this project's connection was actually working (`GET /sites/{tenant_id}` still returned `503` right after). Root cause of the remaining gap, isolated via Supabase MCP (`get_logs`, `get_project`, read-only): a stale Supavisor pooler credential cache, not a capacity issue — same signature as the original 2026-07-07 diagnosis. Password reset + redeploy was performed by the founder himself (not this session — declined to handle the live credential directly when asked). After that redeploy: `GET /health` → `200`; `GET /sites/desert_pro_plumbing_f22725f8` → `404 {"detail":"No site found for this tenant"}` (previously `503`). The `503`→`404` transition is the definitive signal per `site_router.py`'s own design (503 only on a connection failure; 404 means the query reached Postgres and found no row) — confirms `DATABASE_URL` is authenticating for real. **Lesson for future sessions, recorded here so it isn't re-learned the hard way:** the Supabase status page reading "green" does not by itself confirm this project's credentials work — verify against the live app directly.
- #29 — **Real end-to-end verification against the live production backend**, per direct request this session. `POST /intake` against `https://web-staffr3-0-snowy.vercel.app` with a payload clearly labeled as an E2E test submission (`biz_name: "WebStaffr E2E Verification Co"`) → `200`, `submission_id: 1` (first real row in the live `intake_submissions` table since it became reachable), `tenant_id: webstaffr_e2e_verification_co_d07dc1d1`. `GET /sites/webstaffr_e2e_verification_co_d07dc1d1` → `200` with the real submitted data, and confirmed none of the internal-only fields (`license_number`, `lead_routing`, `approver`, `notes`, `competitors`) leaked into the response — consistent with the privacy design from the 2026-07-08 addenda.
- #30 — **Real bug found and fixed in the Lovable "Site Weaver" frontend**, contradicting the 2026-07-07 "Task #13 done" addendum's claim that the frontend was fully wired to the live backend. Loading the live Lovable preview at `/webstaffr_e2e_verification_co_d07dc1d1` showed "Can't reach the server" — network log showed it was calling `http://localhost:8000` (visitor's own machine), never the deployed backend, despite the code (`src/lib/site-api.ts`, `vite.config.ts`) being correct and unchanged. **Root cause (found by Lovable's own AI agent, acting on its own project, not verified independently at the mechanism level by this session):** `API_BASE_URL` had been stored as a Lovable *runtime* secret (only injected server/edge-function-side); Vite only bakes `import.meta.env.*` values from *build-time* sources into the client bundle, so the value never reached the browser bundle regardless of `vite.config.ts`'s `envPrefix` setting. `VITE_API_BASE_URL` wasn't a usable workaround either — `VITE_` is a reserved secret-name prefix in Lovable's own secrets tooling. Fix (by Lovable's agent, commit `5986894b75ab67a89d21ac134a6453d10a49205d`): added a **committed `.env`** file to the Site Weaver project source with `API_BASE_URL=https://web-staffr3-0-snowy.vercel.app` (a public URL, not a secret) so Vite picks it up as a real build-time var.
  - **Important caveat on how this was verified:** Lovable's agent's first attempt (round 1, 1.4 credits) claimed the fix was just a stale-cache dev-server restart and reported "Fixed." This session independently re-tested in a brand-new browser tab with a freshly-issued preview token and found it **still broken**, identical `localhost:8000` failure — i.e., Lovable's own agent's first "verified" claim was wrong, caught only because this session re-tested rather than trusting the report. Sent it back with the disproof; round 2 (3.6 credits) found and fixed the real cause above. This session then independently re-verified again in a third fresh browser tab/token: the page rendered real submitted business data (name, tagline, differentiator, all 3 services, service area, phone, email, dynamic page title, chat widget bubble present, license number correctly absent). Note precisely: at that final check, a live network-request capture had already rolled past the completed request by the time it was queried ("no requests recorded" for that URL pattern) — so the evidence for the final "it works" conclusion is the rendered real data itself (which could only have come from a genuine fetch, since this tenant was invented moments earlier and exists nowhere else), not a captured network log line, unlike the earlier failed-state check where the literal failing request *was* captured live.
  - Total Lovable credits spent: 5.0 (1.4 + 3.6), pre-approved before round 1. No changes were made to this repo (`webstaffr/`) for this item — only to the separate Lovable-hosted project.
  - **Correction, not a rewrite, of the 2026-07-07 "Task #13 done" addendum in `CLAUDE.md`:** that addendum's claim that the frontend was correctly wired and its own preview check ("status: ready, no build error") were both true as far as they went, but did not amount to a working end-to-end fetch in production — the runtime-vs-build-time secret gap above meant the deployed site never actually reached the backend until this session's fix. See the new CLAUDE.md addendum for the full correction note, per this repo's append-only-log convention (prior addenda are not silently edited).

## Blocked (on founder's own decision, not external)

- **GHL signup + `GHL_API_KEY`/`GHL_LOCATION_ID`.** Founder deliberately hasn't signed up yet — it's a 30-day trial and he wants to control when that clock starts. GoHighLevel signup page is open in a browser tab, waiting. Do not proceed until he says he's ready.

## Not yet started

- **`GROK_API_KEY`: set, exercised end-to-end July 12.** Founder added xAI credits at `console.x.ai`; real `POST /chat` call against `https://web-staffr3-0-snowy.vercel.app` returned `200 {"reply":"Hello! This is Angel from your business. How can I help you today?"}`. Confirmed `/chat` path complete: rate-limit table existed in Postgres, ambiguous-column bug fixed in `rate_limit.py` (`721f698`), Vercel redeployed, Grok responded correctly. **`/chat` end-to-end verified — no longer blocked.** Independently re-confirmed same session with a second live call (a substantive question, not a greeting) — got a distinct, dynamically-relevant reply, not the `NullVoiceBackend` canned string — ruling out a false positive.
- **Two hygiene fixes made this session (2026-07-12) to the founder's own parallel commits, both local-only/reversible:** (1) `scripts/apply_rate_limit_migration.py` had a fallback that read `DATABASE_URL` out of `CREDENTIALS.md` if the env var was unset — that file is committed/not gitignored, and every other credential in it is explicitly "name only, never the value, never commit"; the fallback invited a future accidental secret-in-git incident even though nothing was actually leaked (`CREDENTIALS.md` was checked directly and contains no `DATABASE_URL` value). Removed the fallback; script is now env-var-only and fails loudly if unset. (2) `CLAUDE.md`'s `721f698` rewrite had accidentally duplicated the entire `## Security Baseline` section (identical content, back-to-back) and added a `## Approval rules` section that near-verbatim restated the `## Self-Approval Scope` section above it. Removed both duplicates; also moved the "Move on MVP in this order" task list out of `CLAUDE.md` into this file, since `CLAUDE.md`'s own new "Hygiene" rule says TASKS.md is the single source of truth for live status. Not committed yet as of this line.
- Once founder starts the GHL trial: create the Private Integration Token + note `GHL_LOCATION_ID`, wire both into Vercel the same way as `GROK_API_KEY`.
- ~~Delete the throwaway diagnostic scripts~~ — done, `8f7630b` (2026-07-13) removed `test_db_connection.py`, `build_database_url.py`, `test_ghl_connection.py`, `test_grok_connection.py`.
- No auth, CI/CD, or production-readiness decision made yet beyond what's documented in `CLAUDE.md`.

## MVP status: core flow is complete

**Founder decision (2026-07-13): Lovable "Site Weaver" is the canonical frontend, not the local `frontend/` Vite+React scaffold.** Lovable's Site Weaver already has intake → site data → Angel widget embedded and working, verified end-to-end this session (`#29`, `#30`) — chat widget bubble present, real data rendering, no fabricated content. With that decision made, **CLAUDE.md's stated MVP definition (intake → generated customer site → Angel widget embedded and working) is done, in production, right now.**

- `frontend/` (the local Vite+React scaffold) is **parked, not canonical.** It got a real crash-bug + backend-contract fix on 2026-07-13 (see that day's commits) while its status was still undecided — that fix was worth doing at the time and is harmless to leave in the repo, but nothing should be built on top of it going forward unless this decision changes. It has no Angel widget embed and no hosting/deploy story, neither of which is being pursued now.
- Recommended next step, not a blocker: set up GitHub sync on the Lovable project (Workspace settings → Git → GitHub, founder-only OAuth step) so future Site Weaver UX polish is free git commits instead of metered `send_message` calls to Lovable's AI agent. Confirmed via Lovable's own docs this session: credits are only spent on AI-agent messages, not GitHub-synced commits.
- Remaining work is genuinely adjacent, not core-MVP: voice (Retell, built but never exercised against a real phone number/account) and GHL (deliberately not connected, founder's own trial-timing call).

## Reference

- Live backend: `https://web-staffr3-0-snowy.vercel.app` (`/health` confirmed working; DB-touching routes now live as of 2026-07-12 — `/intake` and `/sites/{tenant_id}` verified end-to-end against a real submission, see `#29`)
- Lovable "Site Weaver": project ID `27e51275-323a-4156-a7f9-9bc41c7bf36c` — confirmed rendering real live backend data end-to-end as of 2026-07-12 (see `#30`; required a frontend build-time-secret fix, not just backend availability)
- Latest commit pushed: `9860666` on `origin/main` (2026-07-12, logged the `6dd30fd` push in CLAUDE.md/TASKS.md; verified via `git ls-remote` this session). Note: this line previously read `e487c42`, which was already stale by the time this session started — corrected here, not a claim of a new push this session.
- Full session history and rationale: `CLAUDE.md` (session addenda, chronological, most recent at bottom)
- Findings from the original (pre-`WebStaffr-clean`) repo and Google Drive, re-examined 2026-07-08 for anything useful not carried forward (brand/positioning docs, locked pricing tiers, beachhead niche, unmigrated agent code, CAC/churn/fundraising inconsistencies across Drive, AOKAI lead): `LEGACY_AUDIT.md`. Assessment only — nothing in it has been applied to this repo.

## Non-MVP tooling (informational only, not a blocker)

- 2026-07-08 — Added `WEBSTAFFR_HYBRID_STRATEGY.md` (Cowork/Hermes role split, model cost note) plus two Hermes skills (`ensure-free-llm`, `retell-ai-function-calling`) at the founder's request. Flagged at the time as off the direct MVP path; no code, tests, or git state changed. Not committed/pushed — do that only on explicit approval.
- 2026-07-12 — Built out the full two-layer system that doc sketched, after asking the founder directly whether it was worth prioritizing over MVP blockers (the open question `WEBSTAFFR_HYBRID_STRATEGY.md` had left unresolved) — answer: build it now. Cowork layer: `webstaffr-orchestrator/` at repo root (3 skills, 4 agent specs — corrected against actual project invariants, not the generic template it was sketched from). Hermes layer: `webstaffr-analyze`/`webstaffr-implement` skills added under `~/.hermes/skills/webstaffr/` on the founder's Mac, following the existing `ensure-free-llm` precedent; `~/.hermes/config.yaml` was not touched (`skills.external_dirs: []` already covers the default skills directory, so no config change was needed). Note: `~/.claude/plugins/` — the location the source doc specified for the Cowork plugin — is a Cowork-protected host path and can't be mounted, so the plugin lives in-repo instead (same pattern as `marketing-director-gtm/`); copying it to `~/.claude/plugins/webstaffr-orchestrator/` for auto-load outside this repo is a manual step, not done here. No code, tests, or MVP-path git state changed by this work. Not committed/pushed — do that only on explicit approval.
- 2026-07-12, later — **Superseded the above.** Founder asked to switch the whole system from Cowork+Hermes to native Claude Code. Rebuilt at `.claude/agents/` (4 subagents) and `.claude/skills/<name>/SKILL.md` (3 skills) — project-scoped Claude Code config, verified against `code.claude.com/docs/en/plugins-reference` and `.../sub-agents` this session (not the plugin-manifest route originally sketched; project subagents/skills are simpler and don't need a manifest or install step). `webstaffr-orchestrator/` (the old Cowork plugin) left in place with a superseded notice in its README, not deleted — see `.claude/README.md` for the full rationale and layout. `~/.hermes/skills/webstaffr/*` on the founder's Mac not touched. No code, tests, or MVP-path git state changed. Committed as `6dd30fd` (stacked on the already-local `4e827a9`) and pushed to `origin/main` — verified via `git ls-remote` matching local `HEAD`, no ahead/behind. Left unrelated pre-existing uncommitted state untouched: `scripts/test_db_connection.py`, `.agents/`, `CODE_REVIEW.md`, `skills-lock.json`.
